export enum RolUsuario {
  ADMIN = 'admin',
  USER = 'user',
}

export enum ObjetoImpuesto {
  NO_OBJETO = '01',
  SI_OBJETO = '02',
}

export enum MetodoPago {
  EFECTIVO = 'Efectivo',
  TRANSFERENCIA = 'Transferencia',
}
