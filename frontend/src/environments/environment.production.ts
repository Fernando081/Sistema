export const environment = {
  // En producción se asume reverse proxy en el mismo dominio (/api/v1).
  // Si backend vive en otro dominio, ajustar este valor antes de build.
  apiBaseUrl: '/api/v1',
};
